# AI_RAG_Assistant
A Retrieval-Augmented Generation (RAG) assistant built in Python that scrapes web pages, generates embeddings, stores them in a vector database (Chroma), and answers user queries using a Groq LLM. It allows interactive question-answering over custom web content.
